eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka:8761/eureka/
spring:
  application:
    name: gateway
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081