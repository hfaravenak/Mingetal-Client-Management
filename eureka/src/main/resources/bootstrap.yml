spring:
  application:
    name: eureka
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always